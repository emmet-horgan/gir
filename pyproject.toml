[project]
name = "gir-scripts"
version = "0.1.0"
description = "Scripts and dependencies to help with gir testing"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.3",
    "onnx>=1.20.1",
]
